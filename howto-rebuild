* install
 * prometheus-node-exporter
 * prometheus-snmp-exporter
 * kopia
 * docker
 * rclone
 * vim
 * tmux
 * sudo
 * gpg
 * curl
 * unzip

* Copy over from traefik.hirschnet
 * /home/nat/docker
 * /var/lib/docker/volumes
 * /etc/prometheus
 * /root/.config/*
 * /home/nat/.ssh/*
 * root's crontab

* Misc things to do
 * change static IP on traefik to 18 or something in unifi
 * change static IP on lxc to .12 in unifi
 * stop traefik
 * start irestart lxc traefik
 * rename lxc to traefik
 * docker network create proxy
 * update docenv file
 * start all containers
