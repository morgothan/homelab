# /home/nat/docker/docker-compose.openbao.yml
# OpenBao self-hosted secrets manager (open-source Vault fork).
# Bootstrap: see docs/superpowers/plans/2026-05-20-openbao-migration.md Task 2.
# Normal start: docker compose -f docker-compose.openbao.yml up -d  (auto-starts this stack if unreachable)
# Manual start: docker compose -f docker-compose.openbao.yml up -d

name: openbao

x-logging: &logging
  driver: "json-file"
  options:
    max-size: "10m"
    max-file: "3"
    tag: "{{.Name}}"

services:
  openbao:
    image: quay.io/openbao/openbao:latest
    container_name: openbao
    restart: unless-stopped
    logging: *logging
    ports:
      - "127.0.0.1:8200:8200"
    environment:
      VAULT_ADDR: "http://127.0.0.1:8200"
    volumes:
      - ./openbao/config:/openbao/config:ro
      - openbao-data:/openbao/data
    command: ["bao", "server", "-config=/openbao/config/openbao.hcl"]
    healthcheck:
      test: ["CMD-SHELL", "bao status 2>/dev/null; [ $? -ne 1 ]"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s
    networks:
      - proxy

  openbao-unsealer:
    image: alpine:latest
    container_name: openbao-unsealer
    restart: unless-stopped
    logging: *logging
    depends_on:
      openbao:
        condition: service_started
    volumes:
      - ./.bao-unseal-key:/bao-unseal-key:ro
      - ./openbao/unsealer/unseal.sh:/unseal.sh:ro
    command: ["sh", "/unseal.sh"]
    networks:
      - proxy

volumes:
  openbao-data:

networks:
  proxy:
    external: true
